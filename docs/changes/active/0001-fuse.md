---
id: 1
slug: fuse
title: Fuse — Multi-Model Agent Harness
status: implemented
priority: high
type: feat
created: 2026-08-03
updated: 2026-08-04
claimed_at: 2026-08-04T03:09:31Z
depends_on: []
related: []
discovered_from: []
adrs: []
spec: docs/superpowers/specs/0001-fuse.md
plan: docs/superpowers/plans/2026-08-04-fuse-phase1.md
results: docs/results/2026-08-04-fuse-results.md
trivial: false
auto_groomable: false
branch: feat/fuse
pr: https://github.com/ethanhinson/fuse/pull/1
blocked_by:
reconciled: true
---

## Artifacts

<!-- docket:artifacts:start (generated — do not hand-edit) -->
| Artifact | Link |
|---|---|
| Spec | [0001-fuse.md](https://github.com/ethanhinson/fuse/blob/docket/docs/superpowers/specs/0001-fuse.md) |
| Plan | [2026-08-04-fuse-phase1.md](https://github.com/ethanhinson/fuse/blob/feat/fuse/docs/superpowers/plans/2026-08-04-fuse-phase1.md) |
| Results | [2026-08-04-fuse-results.md](https://github.com/ethanhinson/fuse/blob/feat/fuse/docs/results/2026-08-04-fuse-results.md) |
| PR | [#1](https://github.com/ethanhinson/fuse/pull/1) |
<!-- docket:artifacts:end -->

## Why

We have three strong, independent pieces: a code navigation engine (codeindex), a unified model gateway (LiteLLM at :4000 with 11 models), and a first-class Claude harness (Claude Code). But only Claude gets the full tool suite. DeepSeek, Qwen, Kimi, GLM, and local Ollama models can all use function calling through the gateway — proven by model-council.py — but they run without code navigation, without skills, without hooks, and without session management.

Fuse closes that gap by building a model-agnostic Go agent runtime where Claude is one spoke in the wheel, not the designated orchestrator. Every configured model gets the same tool surface: bash execution, surgical file edits, web search, codeindex blast-radius navigation, and the shared skill ecosystem.

## What changes

- New Go project: `fuse` repository with `fuse` binary
- Core agent loop compatible with any OpenAI-compatible model endpoint (all models via LiteLLM gateway)
- Tool registry: bash, read_file, write_file, edit_file, list_directory, web_search, codeindex_impact, codeindex_callers, codeindex_search, spawn_subagent
- Model registry: all 11 gateway models named (`deepseek-flash`, `qwen-coder`, `kimi`, `claude`, etc.)
- Skills system: SKILL.md discovery from `~/.harness/skills/`, `~/.claude/skills/`, `~/.grok/skills/` (full Claude Code + Grok Build compatibility)
- Hooks: PreToolUse, PostToolUse, Stop, SessionStart, SubagentStop — compatible with Claude Code hook schema
- Subagents: `spawn_subagent` tool enabling multi-model composition within a single session
- Session management: persist, resume, and compress long sessions
- Built-in skills: `/route`, `/compare`, `/docket`, `/impact`, `/explore`, `/summary`
- Configuration via `~/.fuse/config.yml`; per-project `.fuse.local.yml` overrides

## Out of scope

- Web UI or REST API (CLI/TTY only in this change)
- Model fine-tuning, weight management, or local model serving (that's Ollama's job)
- Replacing Claude Code (they coexist; harness is for when you want a non-Claude primary)
- A new plugin marketplace (use skills dirs; marketplace is a future change)
- GUI/IDE integrations (a future change, after the Go SDK is stable)
- Streaming output to programmatic callers (human TTY first)

## Open questions

- Does codeindex need any changes to support being called as a library vs. subprocess from the harness? (Check the Go SDK in code-indexer/sdk/go)
- Should routing live entirely in config rules, or does a small local classifier (e.g., task-keyword → model persona) justify the complexity?
- For `spawn_subagent` with `worktree: true` — does the harness manage worktree lifecycle, or delegate to `git worktree` directly and clean up on SubagentStop?
- Phase 2 timing for hooks and subagents — should they be gated behind the phase 1 smoke test, or developed in parallel by a subagent?

## Reconcile log

<!-- Appended by docket-implement-next's reconcile pass: dated entries of what changed. -->

### 2026-08-04

Reconciled against current reality before planning.

- **Greenfield confirmed.** `origin/main` carries only a managed `.gitignore`; no Go module, no `cmd/`, `internal/`, or `sdk/` yet. Nothing in this change's scope has been built elsewhere — the change stands whole. No `related`/recently-archived changes exist (change 0001 is the first).
- **Toolchain present.** Go 1.26.5 (darwin/arm64) and `codeindex` (`/opt/homebrew/bin/codeindex`) are on PATH, satisfying the two external build dependencies the spec names.
- **Gateway reachable.** LiteLLM at `localhost:4000` answers `GET /v1/models` with 200 (its `/health` path returns 500 — no bearing on the OpenAI-compat surface Fuse uses). The spec's gateway-only model transport is viable now.
- **Env caveat (runtime, not spec):** `LLM_GATEWAY_URL` / `LLM_GATEWAY_KEY` are defined in `~/.zshrc` but are not exported into non-interactive shells. The spec already treats these as config-with-defaults (Auto-detection / zero-config start), so this is a runtime-config detail the build honors via defaults, not a design change.
- **Scope held to Phase 1.** The spec is the full vision (Phase 1 + Phase 2 + full bubbletea TUI). The spec itself designates a **Phase 1 build plan** (§"Phase 1 build plan") as the buildable, architecture-proving slice: core agent loop + gateway adapter, the file/bash/list tool registry, `codeindex_impact`/`codeindex_callers`, the model registry, a basic `fuse --model <name> "<task>"` CLI, loop detection, the skills loader, and `fuse shell`. Phase 2 (hooks, subagents, session persistence, web_search, built-in skills) and the full two-pane mouse TUI remain deferred to follow-up changes. This change = Phase 1, one PR. The plan step breaks Phase 1 into tasks.
- **No design invalidation, no obsolescence, no scope drop.** Reconcile is clean; proceeding to plan.

Follow-up work surfaced (auto-capture disabled — reported, not minted): the deferred Phase 2 surface (hooks, subagents, session persistence, web_search, built-in skills) and the full bubbletea two-pane TUI are natural successor changes a human may want filed after Phase 1 lands.
