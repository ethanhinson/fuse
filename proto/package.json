{
  "name": "fuse-proto-codegen",
  "version": "0.0.0",
  "private": true,
  "description": "buf TS codegen toolchain for the fuse.loop.v1 wire (protoc-gen-es). Run `npm ci` here before `make proto`.",
  "devDependencies": {
    "@bufbuild/protobuf": "2.13.0",
    "@bufbuild/protoc-gen-es": "2.13.0",
    "@connectrpc/connect": "^2.0.0",
    "@connectrpc/connect-node": "^2.0.0",
    "tsx": "^4.19.2"
  }
}
